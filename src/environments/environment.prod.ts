export const environment = {
  production: true,
  /**
   * Set to your deployed Next BFF origin (must allow this SPA origin in CORS_ORIGINS).
   * Example: https://bff.example.com
   */
  bffOrigin: "http://localhost:3001",
};
